#ifndef REG_TYPES_HPP
#define REG_TYPES_HPP

#include <godot_cpp/core/class_db.hpp>

using namespace godot;

void init_mod(ModuleInitializationLevel p_level);
void uninit_mod(ModuleInitializationLevel p_level);

#endif